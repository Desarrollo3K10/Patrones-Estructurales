package Decorator;

public interface Plan_Basico {
    //Declaramos métodos abstractos
    String descripcion();
     double costo();
}
//NOTA: todos los métodos de una interfaz son automáticamente public abstract, aunque no lo escribas.
// PERO la clase que los implemente debe declararlos como públicos si o si*/
